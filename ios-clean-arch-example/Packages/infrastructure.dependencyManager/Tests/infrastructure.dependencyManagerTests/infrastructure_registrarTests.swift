import XCTest
@testable import infrastructure_registrar

final class infrastructure_registrarTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(infrastructure_registrar().text, "Hello, World!")
    }
}
