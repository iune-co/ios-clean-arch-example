import XCTest
@testable import infrastructure_dependencyContainer

final class infrastructure_dependencyContainerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(infrastructure_dependencyContainer().text, "Hello, World!")
    }
}
